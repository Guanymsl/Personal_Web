type User {
  id: ID!
  name: String!
  password: String!
}

type Post {
  id: ID!
  title: String!
  content: String!
}

type Query {
  posts: [Post!]!
}

type Mutation {
  logIn(name: String!, password: String!): User
  createPost(title: String!, content: String!): Post!
}
